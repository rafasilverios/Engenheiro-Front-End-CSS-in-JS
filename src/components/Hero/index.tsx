import { Form, HeroContainer, HeroTitle } from './styles'

const Hero = () => (
  <Form>
    <HeroContainer>
      <HeroTitle>
        As melhores vagas para tecnologia, design e artes visuais.
      </HeroTitle>
    </HeroContainer>
  </Form>
)

export default Hero
